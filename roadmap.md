# Dice!Next 开发计划与更新日志

> 面向测试用户的功能进度页。已完成项目保留可查的压缩摘要；每次发布继续按日期追加，旧日期不会删除。完整设计过程与逐项验收另存于项目内的 `roadmap-legacy.md`。

当前阶段：**Beta 公测与稳定性打磨**

当前已验证构建：**build 868**（2026-08-27）

## 当前进度

| 模块 | 进度 | 当前能力 | 后续重点 |
| --- | --- | --- | --- |
| 掷骰与规则 | ✅ 可用 | 通用骰、COC、BRP、DND、WOD、暗骰、规则速查、角色卡 | 公测兼容性修复 |
| 人物卡与团务 | ✅ 可用 | 多角色卡、自动制卡、`.game`、NPC、HP/先攻/钱币、私聊模拟聊天 | 根据公测反馈继续优化详情页体验 |
| 群管理与日志 | ✅ 可用 | 权限、通知、自动化、模拟聊天、日志管理、跨团日志导出 | 真实 QQ 环境反馈 |
| AI（可选） | ✅ 可用 | 模型管理、润色/翻译、记忆、工具调用、NPC 扮演、识图 | 成本控制与稳定性 |
| 模组与规则包 | 🔧 进行中 | JS 插件、Lua mod、规则包按群启用 | 自定义规则系统、Python 引擎 |
| 平台与面板 | ✅ 可用 | QQ OneBot v11 / QQ 官方机器人 2.0、Discord、KOOK、Web 管理面板、多语言、数据迁移、Release 自动更新 | 多平台实机兼容性与更多 TRPG 规则 |

## 已完成功能摘要

| 状态 | 问题编号 | 功能预期 | 实现结果 |
| --- | --- | --- | --- |
| ✅ | 基础骰系 | 覆盖日常跑团的通用、COC、BRP、DND 与 WOD 掷骰 | 已提供检定、暗骰、奖励惩罚、对抗、成长、理智、先攻、法术位与骰池等能力 |
| ✅ | P0 / P2 | 兼容原版、海豹与常见社区资源 | 已支持 OneDice 兜底、牌堆、规则包、帮助文档、JS 插件、Lua mod、旧版数据导入 |
| ✅ | C#13 | Windows 运行时避免误关骰娘 | 启动后最小化到托盘，并提供托盘退出入口 |
| ✅ | C#24 | 大量黑白名单也能顺畅管理 | 黑白名单分标签并分页，骰娘名单独立显示 |
| ✅ | C#28b / C#40 | 自定义骰娘人格与回复文案 | 人格模板可创建、切换、导入导出，并可在指令页编辑回复 |
| ✅ | C#29 / C#66 | 无代码配置自动回复与条件动作 | 因果规则编辑器、模板、变量、计数器、优先级与测试均已提供 |
| ✅ | C#41 | 帮助文档更易查找 | 支持按来源分组、搜索、编辑、导入导出与规则包帮助合并 |
| ✅ | C#42–44 | 网页模拟聊天可用于真实排查与互动 | 支持图片、表情、戳一戳、引用、历史拉取、撤回标记、持久化与滚动定位 |
| ✅ | C#45 / C#73 | 识别其他骰娘且避免误判 | 按固定横幅识别，并以 `.bot` 探测时间窗限制登记 |
| ✅ | C#46 / C#49 / C#71 | 多语言可扩展且可按群设置 | 支持自定义语言文件、语言关键词、群级回复语言与内建语言元信息 |
| ✅ | C#47–52 | 完善邀请、权限、群与好友管理 | 记录邀请人、对齐群管理权限、用户群门槛、自动清理好友及群管理优化 |
| ✅ | C#53–55 | 方便维护主机与适配器 | `.system` 状态查询；适配器新增/编辑采用渐进式引导 |
| ✅ | C#56–62 | 图片、自动化与退群流程更可靠 | 图片本地化/平台中立码、群自动审核与管理、引用显示、重入群恢复、延迟退群均已完成 |
| ✅ | C#63 | `.rd` 与默认骰行为一致 | 裸 `d` 会使用当前默认骰，显式面数不受影响 |
| ✅ | C#69–70 / C#72 | 自控、防环与戳一戳可配置 | 自消息完整处理且去重防环；戳一戳开关与高级回复界面已优化 |
| ✅ | C#75–76 | OneBot 重连与欢迎消息更稳定 | 重连采用稳定连接后才重置的退避策略；欢迎支持延迟与冷却防刷屏 |
| ✅ | C#78 | AI 基础功能可实际使用 | AI 润色/翻译支持范围控制、结构保护、精细设置与失败回退 |
| ✅ | C#83–87 | AI 更像可靠的跑团助手 | 可真实执行指令/查帮助/写卡，支持分群开关、识图、过滤、自定义人设与正确触发逻辑 |
| ✅ | C#67–68 / A–E | 建立完整可选 AI 对话能力 | OpenAI 兼容模型管理、费用限制、润色翻译、短长期记忆、工具调用、NPC 扮演均已完成 |
| ✅ | 原版指令 / 卡片模板 | 补全常用旧版交互与制卡 | `.me`、`.ak`、`.nnn`、`.link`、`.ob`、`.pc build/redo`、锁卡与自定义模板已实现 |
| ✅ | C#88–92 | 改善 WebUI 的日常管理体验 | 群组、玩家、权限、通知、模组与设置界面完成一批可用性改进 |
| ✅ | C#93–105 | 完善玩家/群详情、日志与角色卡 | 权限页、群文件与日志下载、模拟聊天名称、日志图片去重、卡片布局、自动伤害加值等已完成 |
| ✅ | C#106 | 玩家详情按数据域分栏并支持私聊模拟聊天 | 新增个人信息、人物卡(N)、键值(N)、插件数据(N)、模拟聊天选项卡；私聊记录持久化并可从面板发送 |
| ✅ | C#107–110 | 团务与前端资源体验 | `.game` 团务完整实现，并支持多群接入同一团务会话；回复不再以指令开头；字体本地化；群文件上传已完成 |
| ✅ | D#01–03 | 补齐 DND 与规则包基础能力 | 角色卡衍生属性、伤害加值及相关规则包能力已落地 |
| ✅ | B#01–02 | 改善骰点展示与 WW 骰池计算 | `.dx` 临界骰高亮；`.ww` 支持 `a`/`c`/`+X`、成功骰标注与逐轮爆骰展示 |
| ✅ | D#06–07 | 补齐 DND NPC 战斗、卡片与钱币体验 | 群共享 NPC、代骰、临时 HP、批量先攻；五种钱币自动换算与找零；六项属性与技能检定加值分区保存，新增 `.rdc` 调整值检定 |
| ✅ | D#04–05 / D#08 | 补齐 Lua/JS 兼容层的群名片与私聊回复 | Lua `{card}`、JS `ctx.player.card`/`msg.sender.card` 可读群名片；`seal.replyPerson()` 会向触发者私聊；规则包说明已补齐字段用法。 |
| ✅ | D#09 / D#13 | 对齐 SeaDice 的日志状态与展示 | `logOn`/`logCurName` 可区分进行、暂停与结束；日志状态、名称、恢复提示和 `.log list` 状态标签均已补齐。 |
| ✅ | D#10–12 | 完善模拟聊天文件、规则包卸载与日志导出 | 模拟聊天可上传群文件；规则包重载已串行化并隔离脚本异常；导出中骰娘名使用 `<名称>` 格式。 |
| ✅ | C#111 | 团号更易辨认与抄写 | `.game new` 继续生成 16 位随机团号，但字符集收紧为大写字母与数字；旧团号保持可用。 |
| ✅ | C#112 / 日志管理 | 集中管理日志与多群团务记录 | 数据管理按“日志管理 / 团务管理”分选项卡；日志以可排序表格展示名称、群号、状态、条数、所属团、创建人与时间；团务卡片提供团号、GM、玩家、接入群、状态统计与跨团导出。 |
| ✅ | D#14 | 旧版 Dice! 的 Lua mod 包能正常上传与管理 | 对齐原版安装语义：`<名>.json` 描述档、json+目录成对、纯 json 查询类、仅 model/rulebook 的规则类与一包多 mod 均可上传；启停/删除把 json 与目录成对处理，不再出现删不掉或条目分裂。 |
| ✅ | 多账号群管理 | 同一平台多个骰娘账号在同一个群互不覆盖 | 群设置按“适配器账号”分层：开关、锁定、AI、群名片、欢迎语、插件与当前日志按账号独立，日志列表仍共享；群详情可切换账号查看与修改。 |
| ✅ | Issue #17 | 系统设置支持全局、适配器类型和具体账号三级配置 | 消息形式、欢迎下限、用户群、聊天保留、日志图片、图片发送/图床、响应开关、牌堆显示、事件响应与外部请求等按“账号 > 适配器 > 全局”解析；面板显示来源并可恢复继承。 |
| ✅ | Issue #22 | 连续同级骰子运算符按书写顺序结算 | `d` 运算符改为从左到右结合；`10d10d1` 先计算 `10d10`，再用其结果执行 `d1`，并以计算后的骰数展示最终骰池。 |
| ✅ | Issue #23 | 心跳上报时间遵循系统设置时区 | 时区接口同时返回服务器当前实际生效偏移；心跳卡片将 UTC 上报时间按该偏移换算，包含“跟随系统”模式。 |
| ✅ | Issue #24 | 指令文案分类完整且连续编辑不跳页 | BRP、团务、留言、NPC 等文案进入对应细分类；新增命名空间自动归类与“全部文本”细分类筛选；保存后原位更新并保留滚动位置。 |
| ✅ | Issue #20 | 自动接入 BDC 未验证统计链路 | 适配器登录后自动获取独立未验证心跳 Key 并持续上报；BDC 使用独立数据表和管理员列表，与正式绑定、心跳广场及公开数据完全隔离。 |
| ✅ | Issue #11 | 预览 COC 检定各成功等级文案 | 支持以 `.ra(出目)成功率` 固定 D100 出目；如 `.ra(1)1`、`.ra(100)1` 可直接预览大成功与大失败文案，且不污染骰点与检定统计。 |
| ✅ | 备份稳定性 | 自动备份不再因文件被占用而失败 | 备份跳过旧备份目录、对瞬时占用自动重试，并阻止手动/自动备份并发执行；失败日志会指出具体文件。 |
| ✅ | 群管理权限 | 群管理员可以开关骰子 | OneBot 消息未携带角色时从成员缓存/成员信息补查，并兼容 `administrator` 等角色别名；邀请人与群主原有权限保留。 |
| ✅ | 默认骰修正 | `.rd-5` / `.r-5` 视为“当前默认骰 − 修正” | 省略面数直接跟 ± 修正时按默认骰解析（默认 100 时 `.rd-5` = 1d100−5），不再报负数面数错误。 |
| ✅ | 时区统一 | 上传日志正文按配置时区显示 | 新增“时区”系统设置（默认跟随系统）；日志上传正文、网页导出、审计、备份名、定时任务等统一按配置时区输出，存储仍为 UTC。 |
| ✅ | 消息分段控制 | 分段可整体关闭且顺序稳定 | 新增“消息分段”开关；QQ 官方机器人、Discord、KOOK 不分段；分段按顺序间隔数毫秒发送。 |
| ✅ | 官方机器人骰主 | 支持以 OpenID 添加骰主 | 添加 Master 时平台可选“QQ 官方机器人”并填写 OpenID，群聊/私聊均按 OpenID 识别。 |
| ✅ | GitHub Release 自动更新 | 为大陆与其他网络环境提供可靠检测和下载 | 支持周期 / 手动检查、直连与镜像竞速、大小和 SHA-256 校验、Windows 管理器事务安装与失败回滚；默认只通知。 |
| 🔧 | 官方机器人富媒体 | QQ 官方机器人可直接发送本地图片/文件/视频/语音 | 图片、文件、视频、语音共用 QQ Bot 2.0 分片上传通道；本地文件不再依赖图床；图片已推进到 prepare 阶段，端到端与其余类型待实机确认。 |
| ✅ | 发行资源完整性 | helpdoc、默认牌堆和示例插件随所有平台发行 | 默认内容迁入受 Git 跟踪的只读资源目录；Windows、Linux、macOS 统一打包并强制校验，缺失时 CI 直接失败，不再静默生成残缺包。 |

## 进行中与计划中

| 状态 | 编号 / 方向 | 说明 |
| --- | --- | --- |
| 🔧 | Beta 公测 | 收集多平台实机反馈，优先修复影响日常跑团的问题 |
| 🔧 | 自定义规则 / Python | 继续完善规则系统与 Python 引擎 |
| 📋 | AI 后处理格式保护 | 润色 / 翻译提示词与结果校验需显式保护 Markdown 标记，避免内容正确但富文本结构被改写 |
| 📋 | 混合来源回复格式 | 为内置回复、SealDice JS 插件钩子等混合片段保留各自格式，避免整条消息共用一个格式标记后误解析插件原文 |
| 📋 | 模板变量样式编辑 | 修正已带 Markdown 包装的变量再次套用样式时出现嵌套或残留标记，并补充往返编辑测试 |
| 📋 | 富消息作用域体验 | 全局主开关关闭时明确禁用或解释适配器 / 账号局部开关，避免保存了实际不生效的配置 |
| 📋 | 平台真实预览 | 让指令文案预览更接近 QQ 官方、KOOK、Discord 的实际渲染与降级结果，而不只使用通用 Markdown 预览 |
| 📋 | QQ 官方频道能力边界 | 面板与文档明确区分群聊 / C2C 的 Markdown 和频道消息的纯文本能力，避免把 QQ 官方平台概括成全部支持富文本 |
| 📋 | 作用域文档校正 | 清理仍把消息格式描述为“账号 > 适配器 > 全局”的旧路线图文案；全局富消息主开关应始终具有最高优先级 |
| 📋 | 适配器载荷回归测试 | 补齐 QQ `msg_type`、KOOK 消息类型和 Discord 最终线上内容的传输层测试，不只验证公共格式解析器 |
| 📋 | 平台与规则扩展 | 继续完善 Discord / KOOK 实机兼容性与更多 TRPG 规则；云端能力保持可选 |
| 💤 | QQ 官方机器人 Webhook | 低优先级保留方案；现阶段用户普遍不具备公网 HTTPS 回调条件，继续以 Gateway WebSocket 为默认，仅在形成实际需求后启动实现 |

## 更新日志

### 2026-08-27 · build 871（开发中）

- **GitHub Release 更新器**：新增周期 / 手动检测、自动下载及 Windows 管理器安装重启，默认 6 小时检查并仅通知。
- **大陆镜像可用性**：GitHub 直连与多镜像并行探测，采用首个有效来源并缓存 30 分钟；可选仅直连、仅镜像或自定义 HTTPS 镜像。
- **完整性与回滚**：Release 清单记录五个平台包的准确大小和 SHA-256；客户端校验仓库、版本、平台架构、文件名和压缩路径。Windows 以暂存、事务替换、逆序回滚保护更新，`config/` 与用户数据不参与替换。
- **面板与文档**：关于页新增四语更新状态、下载进度、自动策略和安装确认；同步安装、设置、API、更新日志与路线图。
- **macOS CI 兼容**：更新器后台线程改用 Xcode 15.4 libc++ 可用的 `std::thread + condition_variable`，保留异步排队与安全停止语义，不再依赖缺失的 `std::jthread / stop_token`。
- **旧 Dice! Lua 插件回归**：`loadLua()` 同时支持目录型 mod 的 `script/` 布局和单文件插件的同级资源布局，并使用 UTF-8 路径读取中文脚本；`求签3.0.lua + 求签/*.lua` 可直接运行。
- **插件联网兼容与安全**：旧 Dice! Lua `http.get/post` 恢复默认公网 HTTP(S) 访问，DailyNews 可在加载阶段取数；无论是否严格模式，仍拒绝非 HTTP(S)、内网 / 环回及危险 URL。
- **验证**：服务器完整编译通过；主测试 259 个案例 / 941 个断言、标准 Lua 兼容测试 2 个案例 / 11 个断言全部通过；加载用户提供的求签七签库与 DailyNews 后共 42 个断言通过。

### 2026-08-26 · build 860（开发中）

- **原版指令兼容**：补齐 `.h/.H/.rs`、COC6/7 详细制卡、`.help/.jrrp/.reply on/off`、`.drawh`、`.pc` 旧子指令、`.rule set`、`.mod`、`.user`、`.strXXX` 与 `.cloud` 安全入口。
- **单一数据源**：人物卡、规则书、插件启停、权限、名单与文案继续复用 Dice!Next 的统一存储，不建立平行旧内核。
- **安全边界**：不恢复 `.system cmd`；聊天不执行不可逆玩家 / 插件删除，也不主动拉取外部云黑数据。
- **SealDice JS 事件**：按 `sealdice-core` 对齐加载、消息、指令、发送与平台事件，补全端点、消息 ID、guild / channel、时间、富媒体分段及 @ 状态。
- **验证**：后端 883 项测试全部通过，Release 构建成功。

### 2026-08-22 · build 857（开发中）

- **系统设置重新分区**：管理面板按基础、群事件、回复、数据与图片、网络与云服务、维护、安全与权限重新整理；心跳与日志站归入网络区，图片、图床和聊天记录归入数据区，不再保留“已过时配置项”折叠区。
- **Issue #17 · 配置作用域扩展**：消息发送形式、入群欢迎下限、用户群、聊天记录保留、保存日志图片、图片发送方式、图床、响应开关、牌堆显示、事件响应及外部请求等均支持“全局 / 适配器类型 / 具体账号”，继续按“账号 > 适配器 > 全局”生效；面板显示来源并可恢复继承。
- **按账号执行配置**：出站消息格式、图片发送与图床、聊天记录清理均按消息所属适配器账号读取有效配置；聊天记录新增适配器账号字段，避免同平台多账号互相影响。
- **自动清理好友与群聊**：原好友清理卡片扩展为好友与群聊清理，新增单次清群上限和最大群规模；群人数超过上限或命中已启用的黑名单退群规则时会自动退群，默认安全关闭群规模与黑名单退群。
- **旧 Dice 配置迁移**：`InactiveUserLine`、`GroupClearLimit`、`GroupInvalidSize`、`AllowStranger`、`LeaveBlackQQ`、`DisabledListenAt` 等会迁移到现有功能；好友申请策略可映射为白名单、存在群内使用记录或非黑名单自动通过。私用模式、讨论组自动退出及旧云服务等无效字段不再保留。
- **稳定性修复**：修复配置保存后的延迟防抖线程引用已析构对象、导致测试或短生命周期配置实例随机崩溃的问题；Windows 完整恢复改为保留正在使用的日志目录并安全覆盖其他数据，失败时仍可回滚。
- **验证**：WebUI 构建通过，后端完整测试登记 238 项并全部通过。

### 2026-08-13 · build 844（安全加固 + 插件兼容）

- **JS fetch 默认放行（T8）**：JS 插件 `fetch` 默认对齐海豹裸 fetch并允许公网 HTTP(S)，始终保留协议、SSRF 与危险字符过滤；系统设置“严格模式”可再启用外置 API 开关与白名单。
- **插件卸载清理**：JS 插件删除时同步清理其配置命名空间（ext:<name>:*），防卸载后配置残留。

- **管理面板强制口令**：不再存在“空口令直通”的默认状态——未设置口令时只放行设置流程，其余 API 一律 401；新增首次设置口令引导（前端），默认监听保持 0.0.0.0 不变。
- **接口凭据与 SSRF 加固**：`X-API-Key` 由服务端校验（配置 api_key 时生效，常量时间比较）；聊天图片下载加入 SSRF 黑名单与 curl 配置转义；api_key 停止打印、`config/server.json` 移出 git 跟踪。
- **口令与部署加固**：管理口令 PBKDF2-SHA256 加盐哈希（兼容旧明文迁移）+ 登录失败限速（5 次/60s）；Docker 去特权能力（cap-drop / no-new-privileges / read-only）；NapCat 反向 WS 随机 access_token（两端同步）；备份解压后真实路径二次校验。
- **插件签名（可选）**：配置 PEM 公钥后 JS/Lua 插件上传必须携带 RSA-SHA256 签名，否则拒绝；未配置公钥时行为不变；附签名工具 tools/sign-plugin.py。
- **Lua 插件安全（兼容 Dice!）**：上传 dry-run 预检（descriptor 权限声明 + 静态高危模式扫描）+ 安装前人工确认；sendMsg/HTTP/文件/Shell/原生加载调用写入审计日志（data/audit/lua_audit.jsonl）；cpath 收窄改为可选开关（默认与 Dice! 一致）。
- **JS 插件兼容（对齐海豹）**：补齐 CommonJS `require`、`WebSocket`（drogon 客户端）、`seal.base64ToImage`（MD5 命名写 TempDir）、`.ts` 插件加载（esbuild 优先，简易剥离回退）、`seal.applyPlayerGroupCardByTemplate` 真实实现。
- **CI 安全扫描**：接入 Gitleaks / Semgrep / Trivy 门禁。
- **待实测**：以上均为代码级验证（编译通过），需实机回归——强制口令/登录、图片下载、插件上传确认流、Lua/JS 插件兼容与审计日志、Docker 部署。

### 2026-08-11 · build 828（开发中）

- **QQ 官方机器人 2026-08-10 能力更新**：全部 OpenAPI 调用统一迁移到 `api.bot.qq.com`；Markdown 支持可选图片资源强校验；新增群禁言查询/设置、入群申请列表/审批、自动审批策略查询/创建/修改/删除/执行及白名单管理；`GROUP_JOIN_REQUEST` 已接入骰主通知与现有自动审批逻辑，群详情新增“官方群管”操作页。

- **跑团日志容量与删除**：日志管理表格新增可排序的“占用空间”列，总量由日志数据库已存字段字节数与本地缓存图片实际大小组成，并单独标出图片容量；新增项目样式二次确认删除按钮，删除时同步清理消息、本地图片和各适配器帐号残留的激活日志引用。
- **心跳骰主身份**：正式心跳现在上报骰主昵称与 QQ。默认选取骰主列表中首位 OneBot 骰主，并从玩家档案读取昵称；系统设置可手工填写昵称和 QQ，任一项填写即整组覆盖自动识别，清空两项即可恢复自动模式。
- **Issue #8 · Lua 点歌音乐卡片**：修复 Lua 点歌插件返回 `[CQ:music,...]` 时被 OneBot 当作普通文本发送的问题。出站消息现会生成标准 `music` 消息段，保留 `type`、`id` 以及自定义卡片的链接、音频、标题、简介和封面字段，群聊与私聊均生效。
- **Issue #13 · 插件好感度 API**：JS 插件新增 `seal.favor.get/set/add/grow`，Lua mod 新增 `getFavor/setFavor/addFavor/growFavor`。接口直接读写内置 `player_profiles.favor`，按平台隔离用户；成长调用与 `.favor grow` 相同的概率规则。文档补充完整签名、返回值、多平台注意事项和示例。
- **发行包默认内容恢复**：修复 server/data/ 被 .gitignore 排除、GitHub CI 却仍从该目录寻找资源，导致构建成功但包内没有 helpdoc、默认牌堆和示例插件的问题。默认资源现迁入 server/resources/default-data/ 并纳入版本控制；Windows、Linux、macOS 会统一带上 35 份 helpdoc、2 个 Dice!Next 默认牌堆和 3 个示例 JS 插件，任一必需资源缺失时打包立即失败。本地运行数据不再可能被误带进发行包。

### 2026-08-09 · build 817（开发中）

- **Issue #11 · COC 固定出目文案预览**：兼容海豹的 `.ra(出目)成功率` 语法。`.ra(1)1` 与 `.ra(100)1` 可分别固定出目 1、100，直接查看当前房规及自定义回复对应的成功等级文案；括号内支持骰子表达式，并兼容困难检定、奖惩骰和暗检定。固定出目只用于预览，不写入骰点分布或 `.hiy` 检定统计；`.help ra` 已同步补充用法。
- **Issue #22 · 骰子表达式结合顺序**：修复连续 `d` 运算符被按从右到左解析的问题。同级骰子运算符现与加减乘除一样从左向右结算；例如 `10d10d1` 会先得到 `10d10` 的合计 N，再执行 `Nd1`。动态骰数回执同步显示为 `ND1={...}(N)=N`，并新增连续两个/三个 `d` 以及显式括号右结合的回归测试。
- **Issue #23 · 心跳时间与系统时区一致**：修复系统设置中“上次上报”直接截取 UTC 文本、未进行时区换算的问题。时区接口新增当前实际生效偏移，前端按该偏移显示心跳时间；显式 UTC 偏移和“跟随系统”均生效，不再受浏览器本地时区影响。
- **Issue #24 · 指令文案分类与连续编辑体验**：补齐 BRP、`.game`、`.send`、`.npc` 等指令文案分类，并为未登记的新文案增加命名空间自动归类；“全部文本”新增细分类筛选。保存或重置文案改为原位更新，不再重新加载整页，当前分类、搜索条件、展开状态及滚动位置均会保留。
- **Issue #20 · 自动接入 BDC 云服务**：适配器登录并取得平台账号后，会为当前安装实例与适配器自动申请独立的未验证心跳 Key，并持续上报平台、账号、版本、在线状态和汇总统计。BDC 新增仅管理员可见的“未验证的骰娘”列表；该链路使用独立接口、凭据与数据表，不会合并到正式注册骰娘、心跳广场、展柜或公开 API。适配器配置正式骰娘 API Key 后，将停止其未验证上报。

### 2026-08-06 · build 807

- **QQ 官方机器人富媒体（联调中）**：实现 QQ Bot 2.0 官方分片上传通道（`upload_prepare` → 预签名分片 PUT → `upload_part_finish` → `/files` 合并 → `msg_type=7` 发送），图片、文件、视频、语音共用该通道，本地文件不再依赖图床。已实测图片请求到达 `upload_prepare` 并修复响应解析异常；**图片端到端发送与文件/视频/语音仍需实机确认**。
- **媒体码解析与类型推断**：支持平台中立码 `[img]`/`[file]`/`[video]`/`[voice]` 与 OneBot CQ 码，`url` 优先于 `file`；`file_type` 按类型与扩展名自动选择，未知格式降级为文件；公网 URL 走直传。
- **富媒体稳定性修复**：兼容腾讯接口将数字字段以字符串返回的格式；所有富媒体异步回调增加异常兜底，响应格式异常不再导致进程闪退，并输出逐步日志（prepare/分片/合并/发送）便于定位。

### 2026-08-06 · build 796

- **多账号群管理（同群多个骰娘）**：同一客户端登录多个 OneBot 账号时，后登录账号不再覆盖先前账号的群列表与开关状态。群开关、锁定、AI、群名片、欢迎语、插件状态与当前日志按“适配器账号”独立保存与展示；日志列表仍共享；群详情可切换账号查看与修改，旧数据自动兼容。
- **备份稳定性**：修复自动备份偶发“文件被占用”失败。备份不再把旧备份目录递归打进新包，遇到瞬时占用会自动重试，手动与自动备份不会并发执行；失败提示会指明具体文件。
- **备份排除实例锁**：备份进一步跳过运行时单实例锁文件 `data/.instance.lock`，彻底解决 Windows 上备份必现的“文件被占用”报错。
- **群管理权限**：群管理员现在可以执行 `.bot on/off` 开关骰子。OneBot 消息未携带发送者角色时，会从成员信息补查，并兼容 `administrator` 等角色别名；邀请人与群主原有权限保留。
- **默认骰修正**：`.rd-5` / `.r-5` 按“当前默认骰 − 修正”掷骰（默认 100 时即 1d100−5），不再报 `dice sides must be >= 1: -5`。
- **时区统一**：系统设置新增“时区”（默认跟随系统）。跑团日志上传的正文时间、网页导出、审计、备份文件名、定时任务与 `.time` 等统一按配置时区输出；数据库存储仍为 UTC。修复了上传日志正文显示 +0 时区的问题。
- **消息分段**：系统设置新增“消息分段”开关，可整体关闭；QQ 官方机器人、Discord、KOOK 始终不分段；分段发送按顺序并间隔数毫秒，避免客户端乱序。
- **QQ 官方骰主**：添加 Master 时平台可选“QQ 官方机器人”，填入骰主 OpenID 后即可在群聊与私聊中被识别为骰主。
- **QQ 官方群记录修复**：修复官方机器人群事件按 OpenID 产生“已退群”幽灵记录、群内发言后无法重建记录的问题。群事件统一映射到公共群号；启动时自动清理历史幽灵记录；收到群消息时同步清除删除/退群标记。
- **适配器最后活跃**：适配器详情“最后活跃”不再恒为“—”，改为显示该适配器最近一次收到消息/事件的时间。
- **骰主设置提示**：添加 QQ 官方骰主时下拉选项显示为 OpenID，并提示可用 `.info` 指令查询自己的 OpenID。
- **定时任务按帐号执行**：定时任务“平台”改为“帐号”，下拉列出已配置适配器（以“适配器:ID”显示）；任务可指定具体骰娘帐号发送/退群，旧任务仍按平台回退。
- **消息发送形式按帐号**：全局 Markdown / 富消息开关是最高优先级；全局开启后，可对每个适配器/帐号单独关闭。QQ 官方、Discord、KOOK 各按自己的能力渲染，全局关闭时统一降级传统文本。
- **Markdown 发送性能**：内置、自定义与人格模板在加载 / 保存时生成 Markdown 与纯文字运行时版本，发送前按适配器直接选用；QQ 官方连续两次拒绝卡片后暂时熔断 10 分钟，避免每条消息重复转换或等待失败后二次发送。
- **通知按帐号来源**：通知窗口可指定“全局（所有帐号）”或某个具体适配器帐号；通知按产生它的帐号匹配窗口，并由该帐号发出。
- **通知发送回退顺序**：全局窗口通知按内部适配器编号顺序尝试发送，直到成功一次；帐号来源的通知优先由来源帐号发出，失败（如来源掉线）后按编号顺序回退。

### 2026-07-29 · builds 753–755

- **可选备份与按天保留**：新增“立即备份到服务器”，不再强制浏览器下载；手动与自动备份统一保存在 `data/backups/`，并使用 `dicenext_bak_YYYYMMDD-HHMMSS.zip`。备份会排除该目录，避免把旧备份递归打进新包。可分别选择系统配置、人物卡与数据库、跑团/运行日志、牌堆规则帮助、JS/Lua 插件及本地媒体；局部恢复仅覆盖所选内容，未选数据不会被清空。自动备份沿用相同选择项，清理策略改为保留最近 N 天而非按份数。
- **已保存备份恢复**：备份列表新增“恢复”操作，可直接选取手动或自动保存的 ZIP 校验并暂存；重启主程序后应用恢复，恢复前的数据仍会保留为回滚副本。

### 2026-07-28 · builds 727–752

- **完整备份与恢复**：旧版 Dice! 数据导入统一迁入“备份恢复”页面；新增 ZIP 格式的本地完整备份下载与恢复上传。备份会先刷新 SQLite WAL，并包含当前配置和全部 `data/` 用户数据，生成后还会校验压缩包；备份现统一保存在 `data/backups/`，均可在面板重复下载或删除。自动备份支持按间隔或每日时刻运行，并按保留数量滚动清理。恢复包仅允许受控路径，校验清单后暂存，下一次启动前再安全替换。恢复前的 `data/` 与配置会自动放入回滚副本，避免运行中覆盖数据库或插件文件。
- **跨骰与自响应防误触发**：修复“骰娘账号自响应时 @ 已登记的其他骰娘，`.r/.ra/.pc` 等代骰类指令反而由自己执行”的漏洞。已登记骰娘目标现在优先于代骰参数和 JS 插件例外处理，明确 @ 其他骰娘将始终静默；正常 @ 玩家代骰、同时 @ 本骰或 @全体不受影响。
- **DND `.rdc` 兼容修复**：支持原版紧凑语法 `.rdc3#+1d4力量 15` 及空格写法；附加骰表达式每轮独立掷出，属性按调整值、技能按独立检定加值叠加。回执改为完整算式和阈值比较，如 `D20+1D4+5=18+3+5=26>15 成功`；`.rdc` 帮助同步更新。
- **Windows 发行管理器**：Windows 包改为顶层 `dice-next.exe` 管理器、`app/dice-next-core.exe` 服务核心和集中式 `lib/` 运行库。管理器本身不依赖包内 DLL，直接双击即可启动 core 并继承控制台日志；更新时会复制出临时维护器，待原进程退出后替换包括 `dice-next.exe` 自身在内的完整程序文件，生成回滚目录。设置 `DICENEXT_UPDATE_RESTART=NO` 后，更新完成只退出，不会自行重启 core，适配 MCSM 等进程托管面板。

### 2026-07-27 · build 726

- **定时任务触发方式扩展**：新增「按间隔（每 N 分钟，1–10080）」与「单次（指定日期+时刻，执行一次后自动停用；停机跨过当天则标记过期不补发）」两种触发方式，与原有「每日定时+星期」并列；新建间隔任务从保存时起算一个间隔后首发，修改触发定义会自动重新对表。列表新增「下次执行」列（每日/间隔/单次分别按规则估算）。
- **自定义回复搜索修复**：规则列表搜索现覆盖全部触发条件与全部回复内容（此前多条件规则只能搜到第一条）。

### 2026-07-27 · build 725

- **回复引擎并发加固**：自定义回复与因果规则的内存规则表改为「不可变快照 + 原子替换」——此前网页保存规则触发重载的瞬间，若恰有群消息在匹配，存在数据竞争隐患；现已彻底消除（并发压测通过）。
- **触发限制补全（对齐原版）**：自定义回复新增「每日上限（按窗口计，可设限额提示语）」「冷却提示语（冷却中回一句而不是沉默）」「用户范围（仅指定用户 / 排除指定用户）」；旧版导入同步支持 `limit` 串中的 `today`、`cd@echo`、`user_id`。匹配测试面板可展示提示语回复与「今日已达上限」等跳过原因。
- **不活跃退群并入定时任务**：原「系统设置 · 群不活跃上限」写死每天 04:00 的独立逻辑，现自动迁移为定时任务页一条「全部群（*）」任务——可见、可改时刻/天数/告别语、可停用、可立即执行（尊重条件）；定时任务目标号码支持填 `*` 表示全部群（逐群评估条件后执行）。

### 2026-07-27 · builds 723–724

- **定时任务整修**：任务现在严格按所选平台投递（多平台在线时不再误发到「第一个连上的适配器」）；错过触发分钟不再整天漏发——60 分钟内补发、超窗跳过并留日志（新建/改时刻会自动从明天开始，不会「刚建就补发」）；条件拼错（如 `inactve>=7`）保存时即被拒绝，不再静默变成无条件天天执行；时刻/星期/目标等字段全部校验。网页端新增：平台下拉（读自适配器列表）、可视化条件搭建器、「上次执行」列（今日已执行徽章）、「立即执行」测试按钮（退群任务有确认弹窗，会提示当前条件是否满足）；星期与文案全量 i18n（含日文星期）。
- **自定义回复整修**：普通回复补上原版就有的每条规则「触发概率 / 冷却秒数 / 生效群范围（仅指定群或排除指定群）」；「关键词匹配」正名为「完全匹配」（语义一直是整条消息相等，界面文案误导已修）；同优先级时按精确度排序（完全＞前缀＞包含＞正则，对齐原版）；正则保存时校验并限 400 字（防写错变死规则、防 ReDoS），匹配改用编译缓存；「匹配测试」改为直连真实引擎——所见即所得，并显示每条候选被范围/冷却/概率跳过的原因（可填群号联测范围）。修复：网页开关一条多条件规则会把其余条件/回复静默丢失的合并 bug。
- **回复管线统一**：live 消息、戳一戳映射、Lua eventMsg、网页测试台四处各自手抄的回复兜底链合并为一条（JS插件→Lua因果→因果规则→自定义回复→JS钩子）——此前测试台和 eventMsg 根本打不到因果规则引擎，现已一致。
- **旧版导入增强**：`CustomMsgReply.json` 升级导入对齐原版真实格式——`prefix/search/regex` 词表、旧 `mode+keyword` 写法、`limit` 里的概率/冷却/群名单都能导入（Lua/JS/Py 代码型回复跳过并留日志，此前全部被丢弃）。

### 2026-07-27 · builds 714–722

- **心跳上报（heart.dice.zone 对接）**：新增可选的在线状态上报——在系统设置填入心跳站绑定下发的 access_token 后，骰娘会出现在公开心跳广场并可被搜索，骰主可在心跳站查看在线时长、会话统计并配置离线提醒。客户端内置状态防抖与自动保活，正常退出补报离线、崩溃由服务端超时判定，不会触发心跳站频控。上报间隔 180–480 秒可调，「立即测试」可即时验证。
- **云黑名单（cloudban.dice.zone 对接）**：新增跨骰共享的云端黑名单同步，语义传承原版 Dice! 云黑——上报先待核验、管理员核验通过后才全网生效，注销走墓碑下发。开启后可按周期把云端已核验记录并入本地黑名单（原因带「云黑#编号」前缀、列表显示来源徽章），最低危险等级可设门槛；骰主拉黑可选自动上报云端。凭证复用心跳 access_token。见权限设置页。
- **稳定性修复**：云黑同步分页对齐服务端限流（大名单首次全量同步不再中途失败）、换服务器/调低门槛自动重拉、心跳状态徽章正确区分在线/离线/未上报。

### 2026-07-27 · builds 702–713

- **D#14 旧版 Lua mod 上传/管理修复**：上传对齐原版 `mod` 目录语义——支持 `<名>.json + <名>/` 成对包、纯 `.json` 查询类 mod（可直接上传 .json 文件）、仅含 model/rulebook 的规则类包、一包多 mod 的整库 zip；描述档中的 `helpdoc`/`speech` 均并入。启停与删除会把 `<名>.json` 与目录成对改名/删除（此前只动目录，导致 mod 删后复活、停用后列表分裂无法管理）；`data/plugin` 下的单文件插件同样可启停删除。旧 `/api/mods/upload` 接口不再把 zip 原样落盘。

### 2026-07-26 · builds 648–701

- **QQ 官方机器人 2.0**：新增官方 WebSocket 适配器，支持 AppID + AppSecret 连接与扫码绑定；支持官方群、私聊、主动/被动消息、附件和多官方机器人并存。
- **Discord 与 KOOK**：新增原生 Gateway 适配器，可在面板配置 Bot Token；支持服务器频道、私聊及 `@机器人 指令`。Discord 使用前需在开发者后台开启 Message Content Intent。
- **官方消息与 @ 指令**：兼容群全量消息事件；官方正文中的 `<@OpenID>` 会在进入指令解析前自动剥离，`@机器人 .r`、`@机器人 .bot` 等可正常执行。后台日志新增可读的官方入站消息记录，方便排查消息内容与提及状态。
- **官方身份聚合**：官方群/用户 OpenID 与 OneBot 的真实 QQ 号可关联到同一份人物卡、群设置和日志记录；未关联时使用 13 位虚拟号，避免与真实 QQ 冲突。`.info` 可查看当前窗口的 OpenID、绑定端点与操作指引。
- **安全绑定流程**：默认只能在 OneBot 窗口将当前已验证的 QQ/群与已观察到的官方 OpenID 反向绑定；群绑定须在目标群内由群主或管理发起。官方窗口直绑真实 QQ 改为系统设置中的默认关闭高风险选项，并提供二次确认与风险说明。
- **官方群权限过渡策略**：QQ 官方 API 暂无可信群成员角色字段，基础“群管级”指令暂允许群内用户使用，保证 `.bot on/off` 等日常操作可用；代码已标注，待官方开放角色鉴权后恢复真实校验。骰主与信任级权限不会通过官方 OpenID 映射获得。
- **适配器与面板体验**：官方机器人可选填写真实 QQ 号用于头像展示；群聊/私聊模拟聊天可选择实际发送渠道，便于在同一聚合身份下测试 OneBot 与官方机器人。

### 2026-07-25 · builds 630–647

- **D#07 `.rdc`**：新增 DND 专属调整值检定，支持 `N#`（最多 9 次）、`B/P` 优势/劣势、`±加值`、理由和成功阈值。例如 `.rdc 3# B 力量+2 破门 15`。
- **D#07 DND 角色卡**：六项属性仍保存实际属性值；技能检定加值改为独立存储。`.rdc 力量` 按 `floor((属性值-10)/2)` 计算，`.rdc 运动` 读取技能加值；旧卡技能值会兼容读取，并在下次修改时迁移。
- **多骰 @ 与唤醒**：多骰同 @ 时只要包含本骰便正常执行，不再把另一骰误作代骰对象；`.bot off` 状态下明确 @ 本骰可强制执行指令，WebUI 的“彻底禁用”仍保持完全静默。
- **C#111–112**：新建团号改为 16 位大写字母与数字；日志管理以日志/团务选项卡展示，日志列支持排序，团务保留信息卡和跨团导出。
- **NPC 回执**：`.npc <名称> st ...` 等代执行指令不再沿用操作者的群名片，人物卡更新与掷骰回执统一显示对应 NPC 名称。
- **跨团日志**：团务卡的跨团日志支持 TXT、Excel、网页导出及上传；各格式均按全团时间顺序合并，并在每条对话前保留 `〔场景：群名〕` 标记。
- **Double Cross 兼容**：`.rdx` 作为 `.dx` 的旧版兼容别名，使用完全相同的骰池与爆骰逻辑；帮助系统已列出该别名。

### 2026-07-25 · builds 605–629

- **B#01 `.dx`**：临界骰改为 `<数值,临界值>`，替代尾随 `*`，成功骰更易扫读。
- **B#02 `.ww`**：加入 `a` 加骰阈值、`c` 成功阈值、`+X` 附加成功、⑦⑧⑨⑩ 成功标记与逐轮爆骰展示；旧写法仍兼容。
- **D#06**：补齐 `.npc` 帮助与四语文案，并完成 NPC 代骰、临时生命值、生命值上限夹取及批量先攻体验。
- **D#07**：`.st pp/gp/ep/sp/cp` 按铜币总值结算，扣款时自动找零且不会出现负总额。
- Roadmap 改为“进度 + 历史功能摘要 + 按日期日志”结构；历史日期将持续保留。
- 管理面板的“已完成功能摘要”表格默认收起，可按需展开；版本生成改为编译期源文件，并在打包前校验 exe 内版本，避免包名与 `.bot` 不一致。
- **C#106**：玩家详情改为带数量的个人信息、人物卡、键值、插件数据、模拟聊天选项卡；私聊消息使用独立持久化作用域，可查看历史并从面板发送。
- **D#04–05 / D#08**：Lua 和 JS 均补齐群名片字段与说明；`seal.replyPerson()` 按 SeaDice 语义发送私聊。
- **D#09 / D#13**：日志状态区分进行、暂停、结束；暂停时保留当前日志名，`.log list` 与状态查询显示名称和状态。
- **D#10–12**：模拟聊天支持上传群文件；规则包删除后的重载串行化并隔离脚本异常；日志导出骰娘名统一为 `<名称>`。
- **日志管理 / 跨团日志**：数据管理新增独立日志管理页，支持按群名、日志名、团名和团号检索并导出/删除；团务卡片展示团号、接入群和日志状态。跨团导出会按时间合并所有团群记录，并以 `〔场景：群名〕` 标记场景，避免被染色器当作发言标题；结团后仍可导出历史团务日志。
- **日志管理界面优化**：日志管理与团务管理拆分为选项卡；日志以可排序表格展示日志名、群号、状态、记录数、所属团、创建人及时间；团务卡片补充 GM、玩家、创建时间与进行/暂停/结束日志统计。
- **团务 × 日志**：`.game over` 现与旧版 Dice! 一致，会自动结束仍在进行的日志；暂停中的日志不会被自动结束。
- **跨群团务**：`.game new` 保留显示团名，同时生成不可顺推的 16 位字母数字团号；该团 GM 可在其他群用 `.game open 团号` 展开同一游戏领域，共享 GM、玩家、变量、轮盘和 Lua `GameTable`。`.game close` 仅脱离当前群，`.game over` 结束全部已接入群的团务映射，并收尾各群仍在进行的日志。

### 2026-07-14–15 · builds 600–605

- **C#93–105**：完善权限/玩家/群详情、群文件、日志上传下载、模拟聊天、角色卡布局与自动伤害加值。
- **C#107**：`.game` 团务系统补齐开团、GM、变量、点名、踢人、结团及 Lua 桥接。
- **C#108**：所有默认回复首行避免以 `.` 开头，降低触发其他骰娘的风险。
- **C#109–110**：前端字体资源本地化；补齐群文件上传。
- **D#01–03**：完成 DND 属性／伤害与规则包基础能力等配套改进；D#04、D#05 已根据盲测反馈重新列入修复与文档计划。

### 2026-07-07–10 · builds 531–547

- 完成 AI 分群开关、模型工具化、识图、过滤、人设及触发逻辑优化（C#78、C#83–87）。
- AI 对话完成短期/长期记忆、真实骰子/牌堆/查卡工具与 NPC 扮演；新增白名单、NPC 情绪记忆和识图缓存。
- 完成原版信任等级、骰主通知审计、`.me` / `.ak` / `.nnn` / `.link` / `.ob` 指令补全，以及卡片模板与锁定。

### 2026-07-01–04 · builds 472–479

- 完成 C#13、C#24、C#41–73：托盘、模拟聊天、群管理、图片本地化、语言扩展、自动化、防环与适配器体验改进。
- 建立 OpenAI 兼容模型管理，并完成 AI 润色与 AI 翻译（C#67–68）。

### 2026-06-23–30 · builds 335–437

- 完成 Lua mod 兼容、规则包与帮助文档整合、日志站上传/导出、人格管理、因果规则与文档站。
- 完成 COC / DND 制卡、卡片属性联动、常用指令兼容与网页管理能力扩展。

### 2026-06-17–19 · build 240 起

- 打下 OneBot、Web 管理面板、指令前缀、自定义回复、多条件规则、定时任务、旧版数据迁移与基础骰系的基础能力。

---

后续每次发布均在本节顶端新增“日期 · build 范围”条目；过往日志与已完成摘要保留，方便测试用户追踪版本变化。
