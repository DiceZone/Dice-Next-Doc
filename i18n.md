# DiceNext 多语言（i18n）地基说明

> 目标：机器人回复 **和** Web 前端都能在 **简体中文 / 繁體中文 / English / 日本語** 之间切换。
> 策略：**平台默认 + 可覆盖**（QQ 默认简中、Discord 默认英文，单个群/频道可单独覆盖）。

这份文档解释这套系统怎么运作、文件在哪里、以后想加一句话或加一种语言该怎么做。即使没有编程基础也能照着改。

> 说明：本文讲的是**机器人回复 + Web 管理面板**的 i18n（`server/i18n/` 与 `web/src/i18n/`），**不是本文档站的多语言**。目前正式支持 4 种语言：`zh-Hans`、`zh-Hant`、`en`、`ja`。

---

## 一、核心思路：「一处定义，处处翻译」

机器人和界面里**所有给人看的文字**都不再写死，而是用一个「钥匙（key）」来代表，例如 `dice.crit`。
真正的文字放在每种语言各自的「资源文件」里。要显示时，程序根据「该用哪种语言」去对应的资源文件里把钥匙换成文字。

```
钥匙 dice.crit  ──┬──> zh-Hans.json:  " 【暴击】"
                  ├──> zh-Hant.json:  " 【暴擊】"
                  └──> en.json:       " [Critical Success]"
```

好处：以后要改一句话、加一种语言，只动资源文件，不用碰代码逻辑。

---

## 二、后端（机器人回复）

### 涉及文件

| 文件 | 作用 |
|------|------|
| `server/i18n/zh-Hans.json`、`zh-Hant.json`、`en.json`、`ja.json` | 四种语言的**回复文案**（钥匙 → 文字） |
| `server/src/i18n/i18n.{h,cpp}` | 翻译引擎：加载资源、`tr(语言, 钥匙, 参数)`、找不到时回退 |
| `server/src/i18n/locale_resolver.{h,cpp}` | 决定「这条消息该用哪种语言」 |
| `server/src/common/types.h` | `Locale` 枚举（kZhHans / kZhHant / kEn / kJa）与代码字符串互转 |
| `server/src/core/command_router.h` | 所有指令回复都改成 `i18n_.tr(loc, "钥匙", {...})` |
| `server/config/default_config.json` | `i18n` 区段：默认语言 + 各平台默认语言 |
| `server/src/storage/database.{h,cpp}` | `locale_settings` 表：保存每个群/频道/用户的语言覆盖 |

### 语言怎么决定（解析顺序）

`LocaleResolver::resolve()` 按这个顺序找，找到就用：

1. **该群/频道（或私聊用户）的单独设置** —— 存在数据库 `locale_settings` 表
2. **该平台的默认语言** —— 配置 `i18n.platform_defaults`（onebot_v11→简中、discord→英文）
3. **全局默认语言** —— 配置 `i18n.default_locale`（默认简中）

这些都能**热修改**：改配置文件或数据库后，无需重启。

### 占位符（带变量的句子）

资源里用 `{名字}` 表示要填进去的变量，例如：

```json
"send": { "done": "消息已发送给 Master：{message}" }
```

代码里这样调用：

```cpp
i18n_.tr(loc, "send.done", {{"message", args}});
```

---

## 三、前端（Web 管理面板）

用业界标准 **i18next + react-i18next**。

| 文件 | 作用 |
|------|------|
| `web/src/i18n/locales/zh-Hans.json`、`zh-Hant.json`、`en.json`、`ja.json` | 四种语言的**界面文案** |
| `web/src/i18n/index.ts` | i18next 初始化、记住用户选择（localStorage）、`changeLanguage()` |
| `web/src/components/layout/language-switcher.tsx` | 右上角的语言切换下拉菜单 |
| `web/src/main.tsx` | 在 App 渲染前 `import './i18n'` 完成初始化 |

界面里取文字的写法：

```tsx
const { t } = useTranslation();
return <span>{t('nav.dashboard')}</span>;
```

---

## 四、常见操作怎么做

### 1. 改一句已有的话
找到对应的钥匙（如 `bot.on`），在**四种语言**的 json 里都改一下文字即可（保持钥匙一致，别漏语言）。

### 2. 加一句新的话
- 后端：在 `server/i18n/*.json` 四个文件里加同一个钥匙，然后代码里用 `i18n_.tr(loc, "你的钥匙")`。
- 前端：在 `web/src/i18n/locales/*.json` 四个文件里加，然后 `t('你的钥匙')`。

### 3. 再加一种语言
日语 `ja` **已内置**，可作为新增语言的范本。新增一种语言（如韩语 `ko`）的步骤：
- 后端：复制一份 `en.json` → `ko.json` 翻译；在 `types.h` 的 `Locale` 加对应枚举，并更新 `localeToString/localeFromString`、`i18n.cpp` 的 `supportedLocales()`。也可用**自定义语言**功能免编译加语言（见[帮助与语言](/src/use/help.md)的 `.lang` 与自定义语言包）。
- 前端：加 `locales/ko.json`；在 `web/src/i18n/index.ts` 的 `SUPPORTED_LANGUAGES` 和 `resources` 里加一项。

---

## 五、已完成接口与剩余部分

机器人指令回复、管理面板和群 / 频道语言覆盖已经接入同一套语言列表。当前状态如下：

- [x] 群 / 频道语言覆盖 REST 接口已经完成：`GET /api/i18n/locales` 返回内置与自定义语言；群详情 `PUT /api/groups/{platform}/{groupId}` 的 `locale` 字段可设置或清除覆盖，并即时写入 `locale_settings`。
- [ ] 骰子**算式明细**里的少量标签（如 `COC D100`、`优势`）目前由引擎产生，已可由路由层本地化结果，但 Fate / L5R 的明细字符串仍在引擎内，待迁移。
- [ ] 前端**各功能页**（适配器、骰子规则、回复、设置等）内部的文案尚未逐句抽取。
- [ ] **自定义回复**内容本身的多语言（用户自己写的回复词条是否要分语言）。
